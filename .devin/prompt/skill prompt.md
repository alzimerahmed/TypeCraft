# Prompt 1

1) Download a **"website research & discovery" skill** — a comprehensive, structured research methodology covering every piece of information needed to design and build any website I create in this project and future projects. It must cover:

   - **Project discovery** — project type identification (corporate, e-commerce, portfolio, blog, SaaS, landing page, educational, non-profit, hospitality, real estate, event, personal brand), stakeholder questionnaire, scope document creation, goal definition (sales, leads, awareness, portfolio), target audience profiling
   - **Competitive & reference research** — competitor site analysis (layout, navigation, color, typography, imagery, animation, content tone, CTA strategy, social proof, mobile responsiveness, page speed, SEO structure), industry trend research, award-winning site discovery (Awwwards, CSS Design Awards), visual inspiration gathering, mood board creation
   - **Content strategy** — page-by-page content planning, information architecture, sitemap creation, navigation structure, URL structure planning, SEO keyword research, meta title/description planning, heading hierarchy, schema markup opportunities, brand voice definition, messaging hierarchy, CTA copy direction
   - **Visual design research** — color palette research (primary, secondary, accent, background, text, semantic colors, dark mode variants, color ratios), typography system (heading/body/mono/display fonts, type scale, line height, letter spacing, font loading strategy), spacing & layout system (spacing scale, container widths, grid system, section padding, border radius), imagery direction (photography style, image treatment, illustration style, icon set, video/animation), component design direction (buttons, cards, forms, navigation, modals, hero, footer, testimonials, pricing), motion & interaction design (page load, scroll-triggered, hover, transitions, micro-interactions)
   - **UX & user flow research** — user journey mapping (entry point, first impression, key actions, decision path, conversion point, exit), wireframe descriptions for each page (above the fold, content sections, sidebar, CTA placement, mobile layout), accessibility planning (color contrast, focus states, semantic HTML, alt text, keyboard navigation, ARIA, reduced motion, screen reader testing)
   - **Technical research** — technology stack selection (framework, styling, UI components, animations, CMS, e-commerce, forms, analytics, hosting, database, auth), performance budget (Lighthouse targets, bundle size, image budget, font budget, third-party scripts, caching strategy), technical SEO plan (meta tags, sitemap, robots.txt, canonical URLs, structured data, OG assets, favicons), responsive breakpoint definitions
   - **Deliverables** — compile all findings into a structured research document saved as `research.md` in the project root, with a complete checklist covering scope, competitor analysis, trends, mood board, sitemap, content plan, SEO plan, color palette, typography, spacing, components, motion plan, user journeys, wireframes, accessibility, tech stack, performance budget, technical SEO, and breakpoints

   the skill should reflect modern 2025-2026 research practices — using browser tools to visit and analyze reference sites, web search for industry trends, and structured documentation that can be referenced throughout the entire development process.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 2

1) Download a **"claude taste frontend design" skill** — one that encodes the design taste, preferences, and principles that produce distinctive, intentional, non-generic frontend design for any website I create in this project and future projects. It must cover:

   - **AI slop catalog (what to avoid)** — color anti-patterns (blue primary, blue-to-purple gradients, gray-50 backgrounds, shallow palettes, white cards on gray, oversaturated accents, gradient buttons, colored badge pills, pure black/white text, Tailwind semantic colors), typography anti-patterns (Inter as only font, system-UI stacks, uniform type scales, no letter-spacing, bold for all headings, text-gray-600 body, no display font, uniform line height, no prose styling, centered text everywhere, no font features, all-caps without tracking), layout anti-patterns (three-column equal grids, max-w-7xl everywhere, symmetric padding, template section order, all content centered, equal-height cards, sticky nav with blur, no full-bleed, 4-column footer, no whitespace, no off-grid elements, Z-pattern forever), animation anti-patterns (fade-up on scroll, hover scale 1.05, stagger 0.1s, duration 300ms, ease-in-out everywhere, infinite pulse, parallax hero, counting numbers, typewriter hero, floating icons, skeleton shimmer, no reduced-motion), component anti-patterns (icon+heading+paragraph cards, badge+h1+subtitle+2-button hero, testimonial cards with circle avatars, pricing cards with popular badge, FAQ accordion with chevrons, newsletter input+inline button, stats with big numbers, logo cloud grayscale, CTA with gradient background, tabs with sliding underline, modal with backdrop blur)
   - **Design principles (what to do)** — ground it in the subject (name concrete subject, identify audience, define page's single job), hero as thesis (open with the most characteristic thing, be deliberate), typography carries personality (avoid Inter/Roboto/Open Sans, pair high-contrast faces, use extremes in weight and size, modular type scale with ratio 1.25 or 1.333, contextual line height and letter spacing, optimal line length 60-75ch, enable OpenType features), color & theme (60-30-10 palette construction, distinctive palette sources, neutrals with hue, CSS variables for consistency, separately designed dark mode), layout & structure (vary section padding and max-width, asymmetric grids, left-align by default, generous whitespace, full-bleed elements, mobile-first responsive with clamp() and container queries), motion & interaction (animate only transform and opacity, custom cubic-bezier curves, vary duration by element, under 100ms response time, prefers-reduced-motion support), backgrounds & atmosphere (layered gradients, geometric patterns, micro-gradients, depth with stacked shadows), copy as design material (write from user's side, active voice, be specific not clever, errors don't apologize, empty states are invitations), accessibility as design quality (WCAG 2.2 AA, visible focus states, keyboard nav, semantic HTML, reduced motion)
   - **Craft signals (what makes design look human)** — custom selection colors, custom focus-visible styles, prefers-reduced-motion support, custom scrollbar styling, print styles, proper dark mode, smooth scroll with motion check, hanging punctuation, custom list markers, tabular figures, font smoothing, balanced text wrapping, container queries, custom underline styling
   - **Design process** — brainstorm (compact token system: color, type, layout, signature element), self-critique (check against AI slop catalog, revise generic parts), build (follow revised plan, derive every decision from it), critique again (screenshots, remove one accessory, cut decoration that doesn't serve the brief), restraint (spend boldness in one place, keep everything around it quiet)
   - **Known AI default clusters to avoid** — warm cream background with serif display and terracotta accent, near-black background with acid-green/vermilion accent, broadsheet layout with hairline rules and zero border-radius; also avoid Space Grotesk convergence, blue-to-indigo gradient heroes, rounded-2xl everywhere, backdrop-blur navigation, transition-all duration-300 ease-in-out on every element
   - **Performance as design** — LCP < 2.5s, INP < 200ms, CLS < 0.1, AVIF/WebP image formats, font-display swap with preloading, minimal JavaScript with partial hydration, critical resource preloading

   the skill should reflect modern 2025-2026 frontend design standards — synthesizing Anthropic's official frontend design guidance, modern web design trends, and a comprehensive catalog of AI design anti-patterns. every choice must be grounded in the specific subject, audience, and purpose — if the design could belong to any project, it belongs to no project.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 3

1) Download a **"backend design structural system" skill** — architecture patterns, structural design principles, and engineering best practices for building robust, scalable, maintainable backends for any website or web app I create in this project and future projects. It must cover:

   - **API design** — RESTful conventions, GraphQL schemas, RPC patterns, versioning strategies, pagination, error handling, response envelopes, idempotency
   - **Data modeling** — database schema design, normalization vs denormalization, indexing strategies, relationship modeling, migration patterns, ORM vs raw SQL tradeoffs
   - **Authentication & authorization** — session-based vs token-based auth, JWT best practices, OAuth/OIDC flows, RBAC vs ABAC, API key management, refresh token rotation
   - **Security** — input validation, SQL injection prevention, XSS/XSRF protection, rate limiting, CORS policies, secrets management, dependency auditing, OWASP Top 10
   - **Performance & scalability** — caching layers (in-memory, CDN, query cache), connection pooling, N+1 query elimination, lazy loading, background jobs/queues, horizontal vs vertical scaling
   - **Code organization** — layered architecture vs hexagonal vs clean architecture, separation of concerns, domain-driven design boundaries, module structure, folder conventions
   - **Error handling & observability** — structured logging, error propagation patterns, global error middleware, health checks, distributed tracing, metrics, alerting
   - **Testing** — unit vs integration vs e2e strategies, test pyramids, mocking vs stubbing, contract testing, load testing, test database management
   - **Deployment & DevOps** — environment management (dev/staging/prod), CI/CD pipelines, containerization, infrastructure as code, zero-downtime deployments, rollback strategies
   - **Modern patterns** — serverless functions, edge computing, event-driven architecture, microservices vs monolith tradeoffs, real-time (WebSockets, SSE), streaming responses
   - **Anti-patterns to avoid** — god objects, fat controllers, leaky abstractions, premature optimization, magic strings/numbers, anemic domain models, chatty APIs, missing pagination, hardcoded config, synchronous blocking where async is needed

   the skill should reflect modern 2025-2026 backend engineering standards — not legacy patterns or AI-generated boilerplate. it should encode opinionated, deliberate choices grounded in the specific project's needs, not generic templates.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.

# Prompt 4

1) Download a **"search & conversion optimization" skill** — covering the full spectrum of modern web optimization disciplines for any website I create in this project and future projects. It must cover:

   - **SEO (Search Engine Optimization)** — on-page optimization (meta tags, heading hierarchy, semantic HTML, internal linking, content optimization), technical SEO (core web vitals, crawlability, sitemaps, robots.txt, canonical URLs, schema.org structured data), off-page SEO (backlink strategy, social signals), keyword research methodology, SERP feature targeting
   - **GEO (Generative Engine Optimization)** — optimizing content for AI-powered search engines (ChatGPT, Perplexity, Google AI Overviews), structured data for LLM comprehension, citation-worthy content formatting, entity-based content modeling, fact density and source transparency
   - **SXO (Search Experience Optimization)** — search intent matching, user journey optimization from SERP to conversion, click-through rate optimization, dwell time and engagement signals, zero-click search optimization, rich snippet targeting
   - **AEO (Answer Engine Optimization)** — featured snippet optimization, FAQ schema, question-based content structuring, concise answer formatting (40-60 word blocks), voice search optimization, direct answer targeting
   - **CRO (Conversion Rate Optimization)** — landing page optimization, A/B testing methodology, funnel analysis, heatmaps and session recording analysis, form optimization, CTA design and placement, trust signals, social proof strategies, friction reduction, psychological triggers (urgency, scarcity, reciprocity)
   - **SMO (Social Media Optimization)** — Open Graph and Twitter Card optimization, shareable content structuring, social proof integration, platform-specific content adaptation, UGC strategy, social sharing mechanics
   - **SEM (Search Engine Marketing)** — paid search strategy, keyword match types, ad copy optimization, landing page quality scores, bid strategy, remarketing/retargeting, campaign structure, conversion tracking setup

   the skill should reflect modern 2025-2026 optimization standards including AI search engines, voice search, and evolving SERP features — not outdated keyword-stuffing tactics or generic checklists.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 5

1) Download a **"code review" skill** — a comprehensive, systematic methodology for reviewing code quality, correctness, security, performance, and maintainability across any web project I create in this project and future projects. It must cover:

   - **Correctness & logic** — identifying race conditions, off-by-one errors, null/undefined handling, edge cases, incorrect state transitions, type mismatches, unhandled promises, floating-point precision issues
   - **Security review** — injection vulnerabilities (SQL, XSS, command injection), auth/authz bypasses, insecure dependencies, secrets in code, insecure crypto usage, missing input validation, CORS misconfigurations, OWASP Top 10 coverage
   - **Performance** — N+1 queries, unnecessary re-renders, memory leaks, unbounded loops, missing indexes, large bundle contributions, blocking operations, missing pagination/caching
   - **Architecture & design** — separation of concerns violations, tight coupling, circular dependencies, leaky abstractions, god objects, violation of SOLID principles, inconsistent patterns within the codebase
   - **Code style & readability** — naming conventions, function length, nesting depth, dead code, commented-out code, magic numbers/strings, inconsistent formatting, unclear control flow
   - **Error handling** — swallowed errors, missing error boundaries, inconsistent error propagation, unhelpful error messages, missing try/catch in async flows, unhandled edge cases
   - **Testing review** — missing test coverage for critical paths, brittle tests, tests that don't assert outcomes, missing negative tests, flaky tests, untested edge cases, integration test gaps
   - **Accessibility review** — missing ARIA labels, incorrect semantic HTML, keyboard navigation gaps, color contrast issues, missing alt text, focus management problems
   - **Cross-cutting concerns** — logging adequacy, configuration management, environment-specific behavior, feature flag usage, backward compatibility, migration safety
   - **Review process** — severity classification (blocker, critical, major, minor, nit), constructive feedback framing, prioritized action items, regression risk assessment, deployment readiness verdict

   the skill should reflect modern 2025-2026 code review standards — not just linting checklists but deep structural and logical analysis that catches bugs automated tools miss.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 6

1) Download a **"bug fix & debugging" skill** — a systematic, root-cause-driven methodology for diagnosing and fixing bugs across any web project I create in this project and future projects. It must cover:

   - **Debugging methodology** — reproducing the bug reliably, isolating the failing component, bisecting to find the introducing change, forming and testing hypotheses, binary search through git history
   - **Root cause analysis** — distinguishing symptoms from causes, the 5-whys technique, tracing data flow through the system, identifying the layer where the bug originates (UI, API, database, config, infra)
   - **Common bug categories** — state management bugs (stale closures, race conditions, zombie children), rendering bugs (key warnings, effect dependency issues, hydration mismatches), API bugs (serialization, deserialization, status code handling, timeout), database bugs (transaction isolation, constraint violations, migration drift), CSS/layout bugs (box model, stacking contexts, flexbox/grid edge cases)
   - **Fix discipline** — minimal upstream fixes over downstream workarounds, single-line changes when sufficient, no over-engineering, preserving existing tests, adding regression tests for the fixed bug, avoiding scope creep during a fix
   - **Verification** — confirming the fix resolves the original symptom, checking for side effects, running existing test suite, manual verification of related flows, testing edge cases around the fix
   - **Error reading & interpretation** — reading stack traces, decoding minified errors, interpreting browser console errors, analyzing network failures, reading server logs, understanding error codes
   - **Tooling** — browser DevTools (elements, console, network, sources, performance, memory), debugging proxies, log analysis, database inspection, feature flag toggling for debugging in production
   - **Post-fix practices** — documenting the bug and fix, updating relevant documentation, adding monitoring/alerting for the failure mode, considering whether the bug class could exist elsewhere in the codebase
   - **Prevention** — identifying systemic causes (missing tests, unclear APIs, complex state), recommending guardrails (type safety, runtime validation, linting rules), suggesting architectural improvements that eliminate the bug class

   the skill should reflect modern 2025-2026 debugging practices — not just trial-and-error but structured, evidence-driven diagnosis that finds and fixes the actual root cause.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 7

1) Download a **"testing & QA" skill** — a comprehensive methodology for authoring tests and designing test architecture across any web project I create in this project and future projects. It must cover:

   - **Test strategy & architecture** — test pyramid design (unit > integration > e2e), test matrix planning, deciding what to test at each level, test isolation principles, test data management, test database strategies, parallel vs sequential execution
   - **Unit testing** — test structure (arrange/act/assert), testing pure functions vs side-effectful code, dependency injection for testability, testing async code, testing error paths, parameterized tests, property-based testing, snapshot testing (when to use and when to avoid)
   - **Integration testing** — testing API routes with real or test databases, testing component composition, testing auth flows, testing middleware chains, database integration testing with transactions/rollback, testing external API integrations with mocks/stubs
   - **End-to-end testing** — Playwright/Cypress test design, page object models, testing critical user journeys, testing across browsers, visual regression testing, testing real auth flows, testing file uploads, testing websockets/real-time features
   - **Contract testing** — consumer-driven contract testing (Pact), API schema validation, OpenAPI spec testing, GraphQL schema testing, testing backward compatibility of API changes
   - **Load & performance testing** — k6/Artillery test design, establishing baselines, testing under expected vs peak load, identifying bottlenecks, testing rate limiting, testing database connection limits, soak tests, spike tests
   - **Mocking & stubbing** — when to mock vs stub vs fake vs spy, mocking external services, mocking time/dates, mocking file system, mocking network requests, avoiding over-mocking, testing with real dependencies vs mocked
   - **Test-driven development** — TDD red-green-refactor cycle, BDD with Gherkin, outside-in testing, when TDD adds value vs when it's overhead, testing legacy code that wasn't designed for testability
   - **CI test gates** — pre-commit hooks, CI test pipeline design, flaky test management, test retry strategies, test coverage thresholds, coverage reporting (Istanbul/c8), failing the build on regressions, test parallelization in CI
   - **Visual & accessibility testing** — visual regression tools (Percy, Chromatic), component screenshot testing, axe-core automated a11y testing in CI, color contrast testing, keyboard navigation automated tests

   the skill should reflect modern 2025-2026 testing practices — not just writing tests but designing a test architecture that catches regressions early, runs fast in CI, and provides confidence to ship.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 8

1) Download a **"deployment & DevOps" skill** — a comprehensive methodology for deploying, hosting, and managing infrastructure for any web project I create in this project and future projects. It must cover:

   - **CI/CD pipeline design** — pipeline stages (lint → test → build → deploy), GitHub Actions/GitLab CI configuration, build caching strategies, monorepo vs polyrepo CI, deployment previews (Vercel preview deployments, Netlify deploy previews), automated rollback on failure, branch protection rules, environment-based deployment gating
   - **Infrastructure as code** — Terraform/Pulumi resource management, defining cloud resources declaratively, state management, drift detection, modular infrastructure, environment-specific configurations, secrets in IaC (SOPS, Vault)
   - **Containerization** — Dockerfile best practices (multi-stage builds, layer caching, minimal base images), docker-compose for local dev, Kubernetes deployment patterns (Deployments, Services, Ingress, ConfigMaps, Secrets), health checks in containers, image scanning for vulnerabilities
   - **Cloud provider selection** — Vercel/Netlify for frontend + serverless, AWS (EC2, ECS, Lambda, RDS) for full control, Cloudflare (Workers, Pages, R2) for edge-first, GCP, Azure, self-hosting tradeoffs, cost optimization, vendor lock-in assessment
   - **Environment management** — dev/staging/prod parity, environment variables management, feature flags in different environments, database seeding for staging, environment-specific config files, secrets per environment, promoting builds between environments
   - **Deployment strategies** — blue-green deployments, canary releases, rolling updates, feature-flag-driven releases, zero-downtime deployments, database migrations during deployment, deploy previews for review, automatic deployments on merge vs manual triggers
   - **Monitoring & alerting** — application performance monitoring (Datadog, New Relic, Sentry), uptime monitoring, custom metrics and dashboards, log aggregation (Loki, ELK, CloudWatch), alerting rules and escalation policies, on-call rotation design, error budget and SLO/SLI definition
   - **Domain & DNS management** — domain registration, DNS record management (A, CNAME, MX, TXT), SSL/TLS certificate provisioning (Let's Encrypt, ACM), CDN configuration, custom domain setup on hosting providers, email forwarding, subdomain strategy
   - **Backup & disaster recovery** — database backup strategies (automated snapshots, point-in-time recovery), backup verification and restore testing, disaster recovery runbooks, RTO/RPO definition, multi-region failover, data retention policies

   the skill should reflect modern 2025-2026 DevOps practices — infrastructure as code, GitOps, progressive delivery, and observability-first operations. not manual deployments or click-ops.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 9

1) Download a **"security audit" skill** — a comprehensive methodology for auditing and hardening the security of any web project I create in this project and future projects. It must cover:

   - **OWASP Top 10 deep-dive** — broken access control, cryptographic failures, injection, insecure design, security misconfiguration, vulnerable components, auth failures, software/data integrity failures, logging/monitoring failures, SSRF — for each: detection methods, prevention patterns, testing approaches
   - **Authentication security** — password storage (bcrypt/argon2, never MD5/SHA1), session management, JWT security (algorithm confusion, token leakage, short TTL, refresh rotation), OAuth 2.0 security best practices (PKCE, state parameter, redirect URI validation), MFA implementation, brute force protection
   - **Authorization security** — RBAC vs ABAC implementation, resource-level authorization checks, IDOR prevention, privilege escalation testing, horizontal vs vertical access control, policy enforcement at every layer, API endpoint authorization
   - **Input validation & output encoding** — allowlist vs blocklist validation, SQL injection prevention (parameterized queries, ORM safety), XSS prevention (context-aware output encoding, CSP), command injection prevention, path traversal prevention, SSRF prevention, deserialization attacks
   - **API security** — rate limiting strategies (fixed window, sliding window, token bucket), API key management and rotation, CORS configuration, request size limits, response data filtering, GraphQL security (query depth limiting, cost analysis, introspection disabling in prod), webhook signature verification
   - **Security headers & CSP** — Content-Security-Policy design, Strict-Transport-Security, X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy, COOP/COEP/CORP, cookie security flags (HttpOnly, Secure, SameSite)
   - **Dependency security** — dependency vulnerability scanning (npm audit, Snyk, Dependabot), supply chain attacks prevention, lockfile integrity, reviewing transitive dependencies, SBOM generation, automated dependency update strategies
   - **Data protection** — encryption at rest (database, file storage, backups), encryption in transit (TLS configuration, HSTS), PII identification and handling, data minimization, GDPR/CCPA compliance checklist, right to erasure implementation, data retention policies
   - **Penetration testing methodology** — reconnaissance, scanning, enumeration, vulnerability exploitation, post-exploitation, reporting — adapted for web applications, using tools like Burp Suite, OWASP ZAP, nuclei, and manual testing techniques
   - **Security in CI/CD** — SAST (static analysis) in pipeline, DAST (dynamic analysis) in pipeline, secret scanning (GitLeaks, TruffleHog), container image scanning, IaC security scanning, security gates in deployment pipeline

   the skill should reflect modern 2025-2026 security practices — shift-left security, defense in depth, and automated security testing in CI. not just a checklist but a systematic audit methodology that finds real vulnerabilities.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 10

1) Download a **"performance optimization" skill** — a comprehensive methodology for profiling, analyzing, and optimizing the performance of any web project I create in this project and future projects. It must cover:

   - **Frontend profiling** — Lighthouse auditing (Performance, Best Practices, SEO scores), Chrome DevTools Performance tab (flame charts, main thread analysis, layout thrashing), WebPageTest analysis (waterfall, filmstrip, first byte, start render), Core Web Vitals measurement (LCP, INP, CLS), RUM (Real User Monitoring) with web-vitals library
   - **Bundle optimization** — bundle analysis (webpack-bundle-analyzer, source-map-explorer), tree-shaking verification, code splitting strategies (route-based, component-based), dynamic imports, removing unused dependencies, analyzing transitive dependencies, polyfill management, module federation
   - **Runtime performance** — render optimization (React reconciliation, memoization, virtualization for long lists), layout thrashing detection, forced reflow avoidance, requestAnimationFrame for visual updates, Web Workers for heavy computation, OffscreenCanvas, scheduling APIs (scheduler.yield)
   - **Image & media optimization** — format selection (AVIF > WebP > JPEG/PNG), responsive images (srcset, sizes), lazy loading strategies, image CDN integration, SVG optimization, video optimization (codec selection, lazy loading, poster frames), font optimization (subset, variable fonts, font-display)
   - **Network optimization** — HTTP/2 and HTTP/3 considerations, resource preloading and prefetching, `<link rel="preconnect">` for third-party origins, `fetchpriority` for critical resources, CDN configuration, edge caching, service worker caching strategies (cache-first, network-first, stale-while-revalidate), Brotli/Gzip compression
   - **Database optimization** — query profiling (EXPLAIN ANALYZE), slow query identification, index optimization (composite indexes, partial indexes, covering indexes), N+1 query detection and elimination, query batching, connection pooling configuration, read replicas for read-heavy workloads, materialized views, denormalization for read performance
   - **Caching strategy** — multi-layer caching (browser cache, CDN edge cache, application cache, database cache), cache invalidation strategies (TTL, event-based, tag-based), Redis/Memcached patterns (cache-aside, read-through, write-through, write-behind), cache stampede prevention, cache warming, cache key design
   - **Backend performance** — async/non-blocking I/O, background job processing (BullMQ, Celery), streaming responses for large payloads, pagination strategies (offset vs cursor), GraphQL query complexity analysis and cost limiting, API response compression, server-side rendering performance
   - **Memory optimization** — memory leak detection (Chrome DevTools Memory tab, heap snapshots, allocation timelines), retaining tree analysis, avoiding closures holding references, WeakMap/WeakRef usage, garbage collection understanding, Node.js memory management (--max-old-space-size)

   the skill should reflect modern 2025-2026 performance practices — Core Web Vitals as the north star, RUM data over synthetic tests, and optimization driven by real user impact not theoretical gains.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 11

1) Download an **"accessibility (a11y)" skill** — a comprehensive methodology for designing, implementing, and testing accessible web experiences for any website I create in this project and future projects. It must cover:

   - **WCAG 2.2 compliance** — Level A, AA, and AAA criteria breakdown, perceivable (text alternatives, captions, contrast, resize), operable (keyboard access, no time limits, no seizures), understandable (readable, predictable, input assistance), robust (compatible with assistive technologies), conformance evaluation methodology
   - **Screen reader testing** — NVDA (Windows), VoiceOver (macOS/iOS), JAWS (Windows), TalkBack (Android), testing with screen reader workflow, common screen reader patterns, ARIA live regions for dynamic content, testing forms with screen readers, testing navigation patterns, reading order verification
   - **Keyboard navigation** — tab order management, skip-to-content links, keyboard traps detection, focus management for SPAs (route change focus, modal focus trapping, return focus on close), keyboard shortcuts design, accesskey considerations, testing with keyboard-only navigation
   - **ARIA patterns** — ARIA roles (landmark, widget, document, abstract), ARIA states and properties, when to use ARIA vs semantic HTML (first rule of ARIA: don't use ARIA), common widget patterns (accordion, dialog, menu, tab, combobox, tree) per ARIA Authoring Practices Guide, ARIA live regions, ARIA invalid/required
   - **Cognitive accessibility** — plain language guidelines, consistent navigation, predictable behavior, error prevention (confirm before action, reversible actions), time-out handling, distraction reduction, reading level considerations, content structure for cognitive disabilities, ADHD-friendly design patterns
   - **Visual accessibility** — color contrast (4.5:1 normal text, 3:1 large text, 3:1 UI components), color blindness simulation (protanopia, deuteranopia, tritanopia), not relying on color alone for information, focus indicators (visible, sufficient contrast, minimum size), text resizing up to 200% without loss, high contrast mode support
   - **Motor accessibility** — touch target sizing (minimum 44x44px Apple HIG, 48x48px Material), spacing between interactive elements, drag-and-drop alternatives, gesture alternatives, voice control compatibility, switch device testing, preventing accidental touches
   - **Automated a11y testing** — axe-core integration (browser extension, CLI, CI), Lighthouse accessibility audit, Pa11y, jest-axe for unit tests, Playwright a11y testing, CI/CD a11y gates, what automated tools can and cannot detect (only ~30% of WCAG issues)
   - **Legal compliance** — ADA (Americans with Disabilities Act) web compliance, Section 508 (US federal), EAA (European Accessibility Act), EN 301 549, AODA (Ontario), accessibility statement generation, VPAT (Voluntary Product Accessibility Template) creation, audit documentation
   - **Inclusive design patterns** — designing for situational impairments (one hand, bright sunlight, noisy environment), designing for permanent disabilities, designing for temporary impairments, content design for accessibility, accessible forms (labels, error identification, instructions), accessible tables, accessible motion (reduced motion preferences)

   the skill should reflect modern 2025-2026 accessibility practices — WCAG 2.2 as the baseline, automated testing in CI combined with manual testing, and inclusive design as a philosophy not a checklist.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 12

1) Download a **"content & UX writing" skill** — a comprehensive methodology for writing website copy, microcopy, and content design for any website I create in this project and future projects. It must cover:

   - **Brand voice & tone** — defining brand voice attributes (e.g., authoritative but warm, professional but not stiff), tone variation by context (landing page vs error message vs onboarding), voice consistency across pages, voice do's and don'ts, creating a voice chart, adapting tone for different audiences
   - **Website copywriting** — homepage copy (value proposition, sub-headline, supporting copy), about page copy (story, mission, team), service/product page copy (benefits over features, social proof, objection handling), landing page copy (above the fold, below the fold, CTA), pricing page copy (plan descriptions, FAQ, comparison), blog/article copywriting
   - **Microcopy** — button labels (action-oriented, specific, short), form labels and helper text, error messages (specific, actionable, blameless), success messages, empty states (invitation to act, not mood pieces), loading states, tooltips, placeholder text (never as label replacement), confirmation dialogs, toast notifications
   - **UX writing patterns** — onboarding flow copy (welcome, value explanation, first action, progress), navigation labels (clear, scannable, consistent), menu item naming, breadcrumb copy, pagination copy, filter/sort labels, search placeholder text, no-results copy, account/settings copy, checkout flow copy
   - **Conversion-focused writing** — headline formulas that work, value proposition frameworks, CTA copy variations (primary, secondary, tertiary), urgency without manipulation, social proof copy (testimonials, reviews, usage stats), objection handling in copy, risk reversal copy (guarantees, free trials), scarcity that's honest
   - **Content hierarchy** — information hierarchy in copy (inverted pyramid), scannable content (short paragraphs, subheadings, bullet points), progressive disclosure, above-the-fold priority, reading patterns (F-pattern, Z-pattern), content chunking, summary-first writing
   - **SEO content writing** — writing for search intent (informational, navigational, transactional, commercial), keyword integration without stuffing, title tag and meta description writing, heading structure for SEO, internal linking anchor text, content length decisions, featured snippet optimization, entity-based content writing
   - **Content design** — content-first design (write content before designing layout), content modeling (what content types exist, what fields each has), content templates, content reuse patterns, content governance (who writes, who reviews, who approves), content lifecycle (creation, review, publish, maintain, archive)
   - **Internationalization-ready writing** — writing copy that translates well (avoid idioms, cultural references, humor that doesn't cross cultures), sentence structure for translation, variable handling in copy (pluralization, gender), text expansion accommodation (German ~30% longer than English), RTL-aware copy, locale-specific formatting
   - **Content review process** — content audit methodology, content gap analysis, readability scoring (Flesch-Kincaid, Hemingway), consistency checking (terminology, style guide adherence), stakeholder review workflow, content QA before launch, post-launch content iteration based on analytics

   the skill should reflect modern 2025-2026 content design practices — content as a first-class design material, voice consistency, and writing that serves the user's intent not the company's ego.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 13

1) Download a **"design system & component library" skill** — a comprehensive methodology for building, documenting, and maintaining design systems and component libraries for any web project I create in this project and future projects. It must cover:

   - **Design token architecture** — token taxonomy (primitive/alias/component tokens), color tokens (background, text, border, action, feedback), typography tokens (font family, size, weight, line height, letter spacing), spacing tokens (scale, padding, margin, gap), radius tokens, shadow tokens, motion tokens (duration, easing), z-index tokens, token naming conventions, token transformation pipelines (Style Dictionary, w3c design tokens spec)
   - **Component API design** — prop design principles (explicit, composable, predictable), variant patterns (CVA, class-variance-authority), compound components, slot patterns, render props vs context, controlled vs uncontrolled components, forwardRef patterns, polymorphic components (as prop), component composition over configuration, default prop strategies
   - **Component library architecture** — monorepo setup (Turborepo, Nx), package structure, build configuration (tsup, Vite library mode), dual package ESM/CJS, tree-shakeable exports, CSS-in-JS vs CSS Modules vs Tailwind for libraries, headless UI patterns (Radix, React Aria), separating logic from styling
   - **Documentation** — Storybook setup and configuration, component documentation patterns (usage examples, prop tables, do's and don'ts), interactive playgrounds, design token documentation, design principles documentation, contribution guidelines, changelog generation, versioning documentation, migration guides
   - **Versioning & governance** — semantic versioning for design systems, breaking change management, deprecation strategies, changelog automation (changesets), release strategy (canary, stable, LTS), backward compatibility windows, migration codemods, design system RFC process, contribution model (inner-source vs open-source)
   - **Theme-able components** — multi-brand theming architecture, theme switching at runtime, CSS variable-based theming, Tailwind theme configuration, dark mode as a theme, high-contrast theme, custom theme creation by end users, theme token overrides, scoped theming
   - **Testing components** — component unit testing (testing behavior not implementation), visual regression testing (Percy, Chromatic), accessibility testing per component, snapshot testing strategies, interaction testing in Storybook, cross-browser component testing, component performance testing
   - **Design-to-code workflow** — Figma to code pipeline, design token sync (Figma Tokens plugin, Style Dictionary), component spec handoff, design dev review process, keeping design and code in sync, automated design linting (Figma plugins), design system adoption metrics

   the skill should reflect modern 2025-2026 design system practices — w3c design tokens standard, headless component patterns, automated visual regression, and design-to-code synchronization.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 14

1) Download an **"analytics & tracking" skill** — a comprehensive methodology for implementing analytics, tracking, and measurement infrastructure for any web project I create in this project and future projects. It must cover:

   - **Analytics architecture** — data layer design (event names, properties, user properties), analytics SDK selection (Google Analytics 4, Plausible, Umami, PostHog, Mixpanel), server-side vs client-side tracking, event taxonomy, naming conventions, tracking plan documentation, analytics governance
   - **Event tracking** — pageview tracking for SPAs (route change tracking), custom event tracking (clicks, form submissions, video plays, file downloads), scroll depth tracking, engagement time tracking, outbound link tracking, file download tracking, search tracking, ecommerce tracking (view_item, add_to_cart, begin_checkout, purchase)
   - **Conversion funnels** — funnel definition and measurement, micro-conversions vs macro-conversions, funnel visualization, drop-off analysis, multi-step form tracking, checkout funnel tracking, signup funnel tracking, funnel attribution (first-touch, last-touch, multi-touch)
   - **A/B testing infrastructure** — experiment design (hypothesis, sample size, duration), A/B testing platforms (GrowthBook, PostHog, Optimizely), feature flag integration for experiments, variant assignment, statistical significance, Bayesian vs frequentist approaches, avoiding peeking, experiment tracking in analytics, experiment documentation
   - **Privacy-compliant analytics** — cookieless tracking (Plausible, Umami), GDPR consent management (Consent Mode v2, consent banners), anonymizing IP addresses, data retention policies, do-not-track respect, privacy-friendly alternatives to GA4, first-party analytics hosting, data residency considerations
   - **Tag management** — Google Tag Manager setup, trigger configuration, variable definitions, data layer pushing, custom HTML tags, server-side tagging (SSGTM), tag firing order, tag sequencing, debugging tags (GTM Preview Mode), tag versioning and publishing
   - **User behavior analysis** — session recordings (Hotjar, Microsoft Clarity, FullStory), heatmaps (click, scroll, move), form analytics, rage click detection, dead click detection, user journey analysis, segmentation by behavior, cohort analysis, retention curves
   - **Attribution & measurement** — attribution models (last-click, first-click, linear, time-decay, data-driven), UTM parameter strategy, cross-domain tracking, offline conversion tracking, ROI/ROAS calculation, lifetime value (LTV) tracking, reporting dashboards (Looker Studio, Metabase)

   the skill should reflect modern 2025-2026 analytics practices — privacy-first tracking, server-side tagging, product analytics, and measurement that drives decisions not vanity metrics.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 15

1) Download an **"internationalization (i18n) & localization" skill** — a comprehensive methodology for building multi-language, multi-region web applications for any website I create in this project and future projects. It must cover:

   - **i18n architecture** — translation file structure (JSON, YAML, ICU MessageFormat), translation key naming conventions, namespace organization, lazy-loading translations, translation caching, SSR/SSG considerations for i18n, framework-specific i18n setup (next-intl, react-i18next, i18next, FormatJS)
   - **Locale routing** — URL strategies (subdomain vs path prefix vs query param vs TLD), locale detection (Accept-Language header, geo-IP, cookie, user preference), locale switching without page reload, canonical URLs per locale, hreflang annotations, sitemap per locale, SEO considerations for multi-language
   - **Translation management** — translation workflow (source strings → translation → review → deploy), translation platforms (Crowdin, Lokalise, Phrase), machine translation with human review, translation memory, glossary and terminology management, context for translators (screenshots, descriptions), translation status tracking
   - **Content localization** — locale-aware content (different content per region not just translation), localized images and media, locale-specific layouts, cultural adaptation (colors, symbols, imagery, examples), date/time formatting (Intl.DateTimeFormat), number formatting (Intl.NumberFormat), currency formatting, pluralization rules (ICU MessageFormat plural categories), gender rules, relative time formatting
   - **RTL (right-to-left) support** — CSS logical properties (margin-inline-start, padding-inline-end, inset-inline), flippable layouts, RTL-specific CSS adjustments, bidirectional text handling, RTL icon mirroring, RTL testing methodology, mixed LTR/RTL content (e.g., English text in Arabic page)
   - **Formatting & parsing** — date/time formatting per locale, number formatting (decimal separators, digit grouping), currency display (symbol vs code, position), address formatting per country, phone number formatting, name formatting (given name vs family name order per culture), measurement units (metric vs imperial), paper sizes
   - **Testing i18n** — pseudo-localization (adding accents and lengthening strings to test layout), RTL layout testing, testing with all supported locales, testing translation completeness, testing fallback behavior, testing locale switching, testing SEO per locale, automated i18n linting (missing keys, hardcoded strings)
   - **Deployment & performance** — bundle splitting per locale, lazy-loading translation files, preloading locale on route change, caching translated content, CDN caching per locale, SSG per locale, ISR per locale, translation CDN strategies

   the skill should reflect modern 2025-2026 i18n practices — ICU MessageFormat, CSS logical properties, framework-native i18n, and pseudo-localization in CI.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 16

1) Download a **"database design & optimization" skill** — a comprehensive methodology for designing, optimizing, and managing databases for any web project I create in this project and future projects. It must cover:

   - **Schema design** — entity identification and relationship modeling, normalization (1NF, 2NF, 3NF, BCNF) and when to denormalize, primary key strategies (surrogate vs natural, UUID vs auto-increment vs ULID), foreign key design, junction tables for many-to-many, soft delete patterns, audit columns (created_at, updated_at, deleted_at), enum vs lookup table, JSON/JSONB column usage patterns
   - **Indexing strategy** — B-tree vs hash vs GIN vs GiST vs BRIN index types, composite index design (column order matters), partial indexes (WHERE clause), covering indexes (INCLUDE), expression indexes, index-only scans, avoiding over-indexing, index maintenance and bloat, monitoring index usage, EXPLAIN ANALYZE interpretation
   - **Query optimization** — reading EXPLAIN/EXPLAIN ANALYZE output, identifying sequential scans on large tables, optimizing JOINs (join order, join types — nested loop, hash join, merge join), subquery vs JOIN performance, CTE optimization (materialization vs inlining), pagination optimization (offset vs cursor vs keyset), batch operations, avoiding SELECT *, N+1 detection
   - **Scaling strategies** — read replicas (setup, lag management, read/write splitting), vertical scaling vs horizontal scaling, sharding (hash-based, range-based, geographic), partitioning (range, list, hash), connection pooling (PgBouncer, Pgcat, application-level), query routing, multi-tenancy patterns (shared database, shared schema, isolated schema)
   - **Migration safety** — zero-downtime migration patterns (expand-contract), adding columns with defaults safely, creating indexes concurrently, dropping columns safely (two-phase deploy), renaming columns safely, data backfill strategies, migration testing in staging, rollback planning, migration versioning
   - **ORM optimization** — ORM query inspection (logging raw SQL), avoiding N+1 with eager loading (include, preload, joins), ORM-specific gotchas (lazy loading, the n+1 problem), when to drop to raw SQL, ORM feature tradeoffs (query builder vs full ORM vs raw), connection management in ORMs, transaction management
   - **Data integrity** — constraints (NOT NULL, UNIQUE, CHECK, FOREIGN KEY), triggers for audit logging, database-level validation vs application-level, transaction isolation levels (read committed, repeatable read, serializable), deadlock prevention, optimistic vs pessimistic locking, idempotency keys for operations
   - **Backup & recovery** — automated backup strategies (pg_dump, snapshots, WAL archiving), point-in-time recovery (PITR), backup testing and restore drills, replication for disaster recovery, RTO/RPO definition, multi-region replication, backup encryption, backup retention policies

   the skill should reflect modern 2025-2026 database practices — schema-first design, expand-contract migrations, EXPLAIN-driven optimization, and scaling strategies chosen based on measured evidence not premature optimization.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 17

1) Download a **"migration & refactoring" skill** — a comprehensive methodology for safely migrating, upgrading, and refactoring codebases for any web project I create in this project and future projects. It must cover:

   - **Refactoring methodology** — identifying code smells (long methods, large classes, feature envy, data clumps, primitive obsession, shotgun surgery), refactoring patterns (extract method, extract class, move method, inline function, rename, replace conditional with polymorphism), safe refactoring steps (small changes, test before and after, one refactoring at a time), refactoring without tests (characterization tests, golden master)
   - **Framework upgrades** — upgrade path planning (reading migration guides, assessing breaking changes), incremental upgrade strategies, using codemods for automated migrations, compatibility shims during transition, testing across versions, rollback plan, upgrading major versions step by step, dealing with deprecated APIs
   - **Legacy code migration** — strangler fig pattern (incrementally replace legacy), seam identification (points where behavior can be intercepted), dependency breaking techniques (extract interface, parameterize constructor, wrap method), testing legacy code (characterization tests, pinning tests), dealing with code without tests, dealing with tangled dependencies
   - **Database migrations** — expand-contract pattern (add new schema → migrate data → switch reads → switch writes → drop old), data backfill strategies (batch processing, background jobs), zero-downtime schema changes, handling large table migrations, migration rollback safety, dual-write patterns during migration
   - **Codebase restructuring** — module extraction (identifying bounded contexts, extracting modules), monolith to modular monolith transition, modular monolith to microservices extraction, folder reorganization, import path migration, barrel file management, circular dependency resolution
   - **Breaking change management** — deprecation workflow (deprecate → warn → remove), backward compatibility windows, feature flags for gradual rollout, API versioning during migration, consumer migration tracking, sunset announcements, migration guides authoring
   - **Risk management** — blast radius analysis (what could break), canary deployments for risky changes, feature flag gating, automated rollback triggers, monitoring during migration, staged migration (dev → staging → prod subset → full prod), migration runbooks, rollback runbooks
   - **Automated migration tooling** — codemod authoring (jscodeshift, ts-morph, comby), AST-based transformations, regex-based migrations (and their risks), bulk rename tooling, import path rewriting, automated test generation for migrated code, migration validation scripts
   - **Post-migration verification** — parity testing (old vs new behavior), performance comparison (before vs after), error rate comparison, user-facing behavior verification, cleanup of migration scaffolding (removing shims, compatibility layers, feature flags), documentation updates

   the skill should reflect modern 2025-2026 migration practices — strangler fig pattern, expand-contract migrations, codemod automation, and risk-managed incremental changes over big-bang rewrites.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 18

1) Download an **"animation & motion design" skill** — a comprehensive methodology for designing, implementing, and orchestrating web animations and motion for any website I create in this project and future projects. It must cover:

   - **Animation libraries** — Motion/Framer Motion (declarative animations, layout animations, AnimatePresence), GSAP (timeline, ScrollTrigger, Flip, SplitText), Lottie (After Effects to web, dotLottie), Anime.js, Auto-Animate, when to use which library, library-free CSS-only animations
   - **Scroll-driven animations** — CSS scroll-driven animations (animation-timeline: scroll(), view()), Intersection Observer API for scroll-triggered reveals, ScrollTrigger patterns (pin, scrub, snap), parallax done right (transform-based, not background-position), scroll-linked progress indicators, sticky scroll storytelling
   - **Page transitions** — View Transitions API (cross-document and same-document), route transition patterns (Next.js, Astro, SvelteKit), shared element transitions (shared layout animations), morphing between states, exit/enter animations (AnimatePresence), transition orchestration, browser support and fallbacks
   - **Micro-interactions** — button press feedback (scale, ripple, haptic), toggle/switch animations, accordion/expand-collapse, tooltip entrances, toast notifications (slide, stack, dismiss), drag-to-reorder, swipe gestures, pull-to-refresh, skeleton-to-content reveals, loading spinners vs skeleton screens
   - **SVG animation** — SVG path animation (stroke-dashoffset for draw-on effects), morphing SVG paths, animated icons (Lottie vs SVG vs CSS), SVG filter animations (feGaussianBlur, feDisplacementMap), animated illustrations, SVG sprite animations
   - **Canvas & WebGL** — Three.js fundamentals, React Three Fiber, shaders (GLSL vertex and fragment), particle systems, 3D model loading (gltf/glb), post-processing effects, performance budgets for WebGL, when to use canvas vs WebGL vs CSS, OGL as lightweight alternative
   - **Gesture-driven animation** — drag gestures (pan, with constraints), pinch-to-zoom, swipe navigation, touch vs mouse vs pointer events, spring physics for natural feel, gesture recognition patterns, multi-touch handling, gesture-based navigation
   - **Animation orchestration** — sequencing animations (stagger, chain, parallel), timeline management, animation state machines, coordinating multiple elements, entrance choreography, scroll-driven storytelling sequences, animation cleanup and memory management
   - **Physics-based animation** — spring animations (stiffness, damping, mass), decay animations, fling gestures with physics, momentum scrolling, rubber-band scrolling, natural easing curves, physics-based page transitions
   - **Performance & accessibility** — animating only transform and opacity (GPU-composited), will-change management, avoiding layout thrashing, reduced motion alternatives (instant, fade, none), prefers-reduced-motion implementation, animation pausing when off-screen (Intersection Observer), frame budget (16ms for 60fps)

   the skill should reflect modern 2025-2026 animation practices — View Transitions API, scroll-driven CSS animations, physics-based motion, and motion as communication not decoration.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 19

1) Download a **"media optimization" skill** — a comprehensive methodology for optimizing images, video, and audio for any website I create in this project and future projects. It must cover:

   - **Image formats** — AVIF (best compression, 30-70% smaller than JPEG, wide browser support), WebP (universal fallback, good compression), JPEG XL (next-gen, progressive), JPEG (universal fallback), PNG (lossless, transparency), SVG (vectors, icons, logos), format selection matrix by content type, animated image formats (AVIF animated, animated WebP vs GIF)
   - **Responsive images** — srcset and sizes attributes, art direction with <picture> and <source>, density descriptors (1x, 2x, 3x), width descriptors, breakpoint-based image switching, mobile-first image sizing, avoiding oversized images on mobile, CSS image-set()
   - **Image delivery** — image CDN integration (Cloudinary, Imgix, Cloudflare Images, Vercel Image Optimization, Next.js Image), on-the-fly resizing and format conversion, content-aware compression, smart cropping and face detection, image lazy loading (loading="lazy", Intersection Observer), priority hints (fetchpriority="high" for LCP image), preloading critical images
   - **Image compression** — lossy vs lossless decisions, quality settings per format (AVIF q=50, WebP q=80), perceptual quality metrics (SSIM, Butteraugli), batch optimization tools (Squoosh, sharp, imagemin), metadata stripping, progressive JPEG encoding, image optimization in CI/CD
   - **Video optimization** — codec selection (AV1 best compression, H.265/HEVC good compression, H.264 universal fallback, VP9), container formats (MP4, WebM), adaptive bitrate streaming (HLS, DASH), video encoding settings (CRF, preset, profile, level), video compression tools (FFmpeg, HandBrake), thumbnail/poster frame generation, video lazy loading, video preload strategies
   - **Video delivery** — video CDN (Cloudflare Stream, Mux, Cloudinary Video), adaptive streaming setup, video player selection (Video.js, Plyr, native <video>), video player customization, captions and subtitles (WebVTT, SRT), video analytics, video thumbnails, preview-on-hover, background video optimization
   - **Audio optimization** — audio formats (Opus best, AAC universal, MP3 fallback), audio compression settings, audio CDN delivery, audio lazy loading, audio streaming, podcast audio optimization, audio sprites for game/UI sounds
   - **Next-gen media** — HDR images (AVIF HDR, JPEG XL HDR), wide gamut images (Display P3, Rec. 2020), 3D model optimization (Draco compression, mesh simplification), 360-degree images and video, depth maps and parallax images
   - **Media pipelines** — upload processing pipelines (resize → compress → format-convert → CDN), automated alt text generation (AI), image focal point detection, automatic cropping strategies, batch processing in background jobs, media library management, media backup strategies

   the skill should reflect modern 2025-2026 media practices — AVIF-first, adaptive streaming, image CDN pipelines, and Core Web Vitals-driven optimization.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 20

1) Download a **"state management & data fetching" skill** — a comprehensive methodology for managing application state and data fetching for any web project I create in this project and future projects. It must cover:

   - **State categorization** — server state (cached data from APIs, needs sync, single source of truth is the server), client state (UI state, form state, toggle state, lives in browser), URL state (filters, pagination, modal open, shareable), persistent state (localStorage, IndexedDB, cookies), ephemeral state (toasts, hover, focus), deriving the right category for each piece of state
   - **Server state management** — React Query/TanStack Query (queries, mutations, invalidation, optimistic updates, prefetching, infinite queries, paginated queries, parallel queries, dependent queries, cache keys, stale time vs cache time, background refetch), SWR (simpler alternative, revalidation strategies), Apollo Client cache (normalized cache, cache policies, fetch policies), URQL, Relay
   - **Client state management** — Zustand (minimal, no boilerplate, selectors, middleware), Redux Toolkit (when complexity demands it, slices, createAsyncThunk, RTK Query), Jotai (atomic state, derived state), Valtio (proxy-based), Nanostores (framework-agnostic, tiny), Context + useReducer (for simple cases), state colocation principle, when to use global vs local state
   - **URL state** — URL as source of truth (searchParams for filters, pagination, sort), Next.js useSearchParams, React Router useSearchParams, URL state synchronization, shareable URLs, back/forward button support, SSR-friendly URL state, nuqs library for type-safe URL state
   - **Form state** — React Hook Form (register, handleSubmit, validation, watch, useFieldArray, form context), Formik, Zod validation integration, controlled vs uncontrolled inputs, form state persistence, multi-step form state, draft/saved form state, form autosave
   - **State machines** — XState/Stately (finite state machines, statecharts, hierarchical states, parallel states, guards, actions, invoked services), when to use state machines vs reducers, modeling complex flows (auth, onboarding, checkout, multi-step wizards), state machine testing, visual state machine design
   - **Optimistic updates** — optimistic UI patterns (update UI immediately, rollback on error), React Query optimistic updates, conflict resolution, retry with backoff, error recovery UI, optimistic vs pessimistic update tradeoffs
   - **Cache invalidation** — cache invalidation strategies (time-based, event-based, manual), React Query invalidation patterns, stale-while-revalidate, background refetch triggers (window focus, reconnect, interval), cache eviction policies, cache key design, normalized vs non-normalized caches
   - **Data fetching patterns** — fetching in React Server Components vs Client Components, streaming with Suspense, prefetching (route prefetch, link hover prefetch, intent-based prefetch), waterfall vs parallel fetching, dependent queries, polling, real-time data sync, infinite scroll vs pagination, cursor pagination
   - **Offline & sync** — offline state management, optimistic updates with background sync, conflict resolution strategies (last-write-wins, CRDTs, operational transform), queue mutations when offline, replay on reconnect, IndexedDB for offline cache

   the skill should reflect modern 2025-2026 state management practices — server state separated from client state, URL as source of truth, state machines for complex flows, and React Server Components changing the state management landscape.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 21

1) Download a **"PWA & offline-first" skill** — a comprehensive methodology for building progressive web apps and offline-capable web applications for any website I create in this project and future projects. It must cover:

   - **Service workers** — service worker lifecycle (install, activate, fetch, message), registration strategies, update flow (skipWaiting, clients.claim), service worker debugging (Chrome DevTools Application tab), workbox integration (precache, runtime cache, strategies), service worker scope, multiple service workers, service worker unregistration
   - **Caching strategies** — cache-first (for static assets), network-first (for fresh content), stale-while-revalidate (best of both), network-only (for non-cacheable), cache-only (offline fallback), offline fallback pages, cache expiration (max entries, max age), cache versioning and cleanup, range request caching (video/audio)
   - **Web app manifest** — manifest.json configuration (name, short_name, icons, start_url, scope, display, orientation, theme_color, background_color), maskable icons, shortcut items, screenshot for install prompt, display modes (standalone, fullscreen, minimal-ui, browser), manifest validation
   - **Installability** — install criteria (HTTPS, service worker, manifest, icons), beforeinstallprompt event, custom install UI, install banner patterns, iOS add-to-home-screen, desktop PWA installation, app store deployment (Google Play via TWA, Microsoft Store, Apple App Store via PWA), PWA vs native tradeoffs
   - **Offline data storage** — IndexedDB (structured client-side storage, indexes, transactions, cursors), IndexedDB libraries (Dexie, idb, localforage), IndexedDB vs localStorage vs sessionStorage vs cookies, data synchronization, storage quotas and eviction, persistent storage requests, encrypted offline storage
   - **Background sync** — Background Sync API (defer actions until connectivity), Periodic Background Sync, background fetch (large downloads), sync event in service worker, retry strategies, sync registration, sync vs push notifications for background work
   - **Push notifications** — Web Push API, notification permissions (request timing, denial recovery), notification display (actions, badges, tags, renotify, silent, vibrate), notification click handling, push subscription management, VAPID keys, push service selection, notification analytics, notification preferences management
   - **Offline-first architecture** — offline-first data patterns (local-first, sync later), conflict resolution (last-write-wins, CRDTs, operational transformation), sync engine design, sync queue management, partial sync, delta sync, sync status UI, offline indicator UI, optimistic UI with offline support
   - **App shell model** — app shell caching (HTML, CSS, JS, fonts), instant loading with cached shell, dynamic content fetching, app shell vs server-rendered, hybrid approaches, app shell updates
   - **PWA testing** — Lighthouse PWA audit, PWA Builder testing, manual offline testing (Chrome DevTools offline simulation), service worker testing, notification testing, install testing, cross-browser PWA support matrix

   the skill should reflect modern 2025-2026 PWA practices — workbox for service worker management, local-first architecture, push notifications, and app store deployment via PWA.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 22

1) Download a **"payment & commerce integration" skill** — a comprehensive methodology for integrating payments, subscriptions, and e-commerce functionality for any web project I create in this project and future projects. It must cover:

   - **Payment gateways** — Stripe (Payment Intents, Checkout Sessions, Elements, Payment Element, embedded checkout), PayPal, Adyen, Braintree, Razorpay, selection criteria (fees, geography, features, ease of integration), multi-gateway routing, marketplace payments (Stripe Connect, split payments)
   - **Checkout flow design** — one-page vs multi-step checkout, guest checkout, express checkout (Apple Pay, Google Pay, Link, PayPal), cart to checkout handoff, shipping calculation, tax calculation, discount code application, order summary, order confirmation, abandoned cart recovery
   - **Subscription management** — recurring billing setup, subscription tiers and plans, trial periods, proration, upgrades/downgrades, cancellation flows, dunning management (failed payment retry), subscription analytics (MRR, churn, LTV), Stripe Billing vs Stripe Subscriptions, usage-based billing
   - **Webhook handling** — webhook endpoint design, signature verification, idempotency (handling duplicate webhooks), webhook retry logic, webhook testing (Stripe CLI, ngrok), event types to handle (payment_succeeded, payment_failed, subscription_updated, etc.), webhook security, dead letter queue
   - **PCI compliance** — SAQ-A (fully outsourced, Stripe/PayPal hosted), SAQ-A-EP (redirect), PCI DSS requirements overview, never storing card data, tokenization, 3D Secure / SCA (Strong Customer Authentication), PCI compliance for subscriptions
   - **Tax & invoicing** — tax calculation (Stripe Tax, TaxJar, Avalara), tax nexus, VAT/GST handling, digital vs physical goods tax, invoice generation, PDF invoices, receipt emails, tax reporting, international tax considerations
   - **Fraud prevention** — Stripe Radar, Address Verification System (AVS), CVV verification, velocity checks, IP geolocation, device fingerprinting, manual review workflows, chargeback management, fraud signals, blocklists
   - **Multi-currency** — presentment currency vs settlement currency, currency conversion, rounding rules, multi-currency pricing, currency display formatting, exchange rate management, currency switching UI
   - **Refund & dispute management** — full vs partial refunds, refund timing, dispute/chargeback response, evidence submission, win rate optimization, dispute prevention, customer communication during disputes
   - **Order management** — order lifecycle (pending → paid → fulfilled → shipped → delivered), order database schema, order status tracking, order history, order modifications, returns and exchanges, order notifications

   the skill should reflect modern 2025-2026 payment practices — Stripe-first integration, Payment Intents API, SCA/3D Secure compliance, webhook-driven architecture, and subscription billing best practices.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 23

1) Download a **"file handling & media uploads" skill** — a comprehensive methodology for implementing file uploads, processing, and management for any web project I create in this project and future projects. It must cover:

   - **Upload UI/UX** — drag-and-drop zones (dragenter, dragover, dragleave, drop events), file picker integration, paste-to-upload (clipboard API), multiple file selection, file preview (images, videos, PDFs), upload progress UI, upload queue management, cancel/retry uploads, file size display, upload error states
   - **Upload strategies** — direct to server (multipart/form-data), direct to S3/cloud storage (presigned URLs), upload via CDN edge, chunked uploads (split large files, parallel chunks, resumable), tus protocol (resumable uploads), upload orchestration, bandwidth management
   - **File validation** — client-side validation (file type, size, dimensions, duration), server-side validation (never trust client), MIME type verification, magic number verification, file name sanitization, virus scanning (ClamAV), max file size enforcement, dimension/duration limits
   - **Image processing** — client-side image resizing (Canvas API, OffscreenCanvas, createImageBitmap), client-side compression (canvas.toBlob quality), EXIF reading and stripping, image cropping UI (react-image-crop, Cropper.js), image rotation, thumbnail generation, focal point selection, image filters
   - **File storage** — S3/Cloudflare R2/Google Cloud Storage, bucket organization, file naming strategies (UUID, content hash, original name), CDN integration for file delivery, signed URLs for private files, file lifecycle policies, storage cost optimization, multipart uploads for large files
   - **Presigned URLs** — generating presigned URLs server-side, direct browser-to-S3 uploads, URL expiration times, conditional uploads (Content-Type, Content-Length), upload policy restrictions, CORS configuration for S3, error handling for failed uploads
   - **Video processing** — server-side transcoding (FFmpeg, AWS MediaConvert, Cloudflare Stream), thumbnail extraction, video metadata extraction, video duration limits, audio extraction, video watermarking, adaptive bitrate encoding, upload progress for large videos
   - **Document handling** — PDF generation (server-side, client-side with pdf-lib), PDF preview, PDF text extraction, document conversion (docx to PDF, etc.), OCR (Tesseract.js, Google Vision), document signing, document versioning
   - **File management** — file library UI, file organization (folders, tags, search), file metadata storage, file access control, file sharing (public links, expiring links, password-protected), file deletion and cleanup, duplicate detection, storage usage tracking

   the skill should reflect modern 2025-2026 file handling practices — presigned URL uploads, client-side preprocessing, chunked/resumable uploads, and CDN-backed delivery.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 24

1) Download a **"search & filtering architecture" skill** — a comprehensive methodology for implementing search, filtering, and discovery for any web project I create in this project and future projects. It must cover:

   - **Client-side search** — Fuse.js (fuzzy search), FlexSearch (fast full-text), Minisearch, Lunr.js, when to use client-side search (small datasets, offline, instant), index building and updating, search performance in the browser, Web Worker for search offloading
   - **Server-side search** — PostgreSQL Full-Text Search (tsvector, tsquery, GIN indexes, to_tsvector, plainto_tsquery, websearch_to_tsquery, ranking with ts_rank), Meilisearch (typo tolerance, instant search, easy setup), Typesense (fast, typo-tolerant, faceted), Elasticsearch/OpenSearch (enterprise-grade, complex queries), Algolia (hosted, fast, expensive), selection criteria
   - **Faceted search** — faceted navigation UI (checkboxes, ranges, color swatches), facet computation server-side, multi-select facets, facet counts display, hierarchical facets, clearing filters, active filter display, facet URL state, facet performance optimization
   - **Autocomplete & typeahead** — debounced search input, suggestion dropdown UI, keyboard navigation (arrow keys, enter, escape), search-as-you-type, recent searches, popular searches, suggestion ranking, instant results preview, command palette patterns (cmdk, Raycast-style)
   - **Filter UI patterns** — sidebar filters, top bar filters, mobile filter drawer, filter chips/tags, range sliders, date range pickers, sort dropdown, results count, empty results state, loading state during filter, URL-synced filters, shareable filtered URLs
   - **Search relevance** — relevance tuning (field weights, boosting, custom ranking), typo tolerance configuration, synonym management, stop words, stemming and lemmatization, language-specific analyzers, search analytics (click-through, zero-result rate, search abandonment)
   - **Search performance** — search index optimization, index updates (real-time vs batch), search result caching, pagination of results, lazy-loading results, infinite scroll for search, search debouncing, precomputing search indexes, edge search (Cloudflare, Algolia)
   - **Search UI/UX** — search results layout (grid, list, map), result highlighting (matching terms), result previews, sorting options (relevance, date, price, popularity), search refinements, related searches, "did you mean" suggestions, empty state design, search pagination
   - **E-commerce search** — product search patterns, SKU search, variant search, search by category, price range filtering, availability filtering, product recommendations, search merchandising, search-based product discovery, search A/B testing

   the skill should reflect modern 2025-2026 search practices — Meilisearch/Typesense for self-hosted, Algolia for hosted, PostgreSQL FTS for simple needs, and command palette patterns for app search.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 25

1) Download a **"real-time & WebSockets" skill** — a comprehensive methodology for implementing real-time features, live updates, and collaborative experiences for any web project I create in this project and future projects. It must cover:

   - **WebSocket implementation** — WebSocket API (client-side, connection lifecycle, reconnection), ws/uWebSockets.js (Node.js server), WebSocket libraries (Socket.io, Soketi, Centrifugo), connection authentication (query params, headers, cookies, JWT), connection management, room/channel management, broadcasting patterns
   - **Server-Sent Events (SSE)** — EventSource API, SSE vs WebSockets (unidirectional vs bidirectional, auto-reconnect, HTTP/2 multiplexing), SSE for notifications and live updates, SSE with POST (fetch-based streaming), SSE scaling considerations, SSE in Next.js/Edge
   - **Real-time sync patterns** — live data updates (broadcast changes to all clients), presence detection (online/offline, typing indicators), live cursors/pointers, real-time dashboards, live scores/notifications, optimistic UI with real-time confirmation, delta updates vs full state sync
   - **Reconnection strategies** — exponential backoff, jitter, max retries, offline queue (store messages during disconnect), reconnect event handling, connection state UI (connected, reconnecting, offline), graceful degradation to polling, connection heartbeat/ping-pong
   - **Scaling WebSockets** — WebSocket scaling challenges (single server limit), Redis pub/sub for multi-server broadcast, WebSocket adapter (Socket.io Redis adapter), sticky sessions with load balancer, WebSocket-aware load balancing, horizontal scaling with message broker (Redis, NATS, Kafka)
   - **Real-time collaboration** — CRDTs (Conflict-free Replicated Data Types — Yjs, Automerge), Operational Transformation (OT), real-time text editing (CodeMirror, TipTap, ProseMirror with Yjs), real-time canvas collaboration, cursor sharing, selection sharing, awareness protocol, undo/redo in collaborative editing
   - **Push from server** — server push patterns (event-driven, polling, long polling, SSE, WebSocket), when to use each, push notification integration (Web Push API + service worker), email + push + in-app notification fan-out, notification routing and preferences
   - **Message protocols** — message format design (JSON, MessagePack, Protobuf), message types (event, command, query, ack), message ordering guarantees, message compression, message size limits, binary vs text frames, message batching
   - **Security** — WebSocket authentication (token in handshake, per-message auth), origin validation, rate limiting per connection, message validation, preventing WebSocket hijacking, CORS for WebSockets, connection limits per user
   - **Testing real-time** — testing WebSocket connections, simulating multiple clients, testing reconnection, testing message ordering, load testing WebSockets (Artillery, k6), testing collaboration conflicts, E2E testing with real-time features

   the skill should reflect modern 2025-2026 real-time practices — SSE for simple push, WebSockets for bidirectional, CRDTs for collaboration, Redis pub/sub for scaling, and graceful degradation strategies.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 26

1) Download a **"git workflow & version control" skill** — a comprehensive methodology for managing version control, branching, and collaboration for any web project I create in this project and future projects. It must cover:

   - **Branching strategies** — trunk-based development (main branch, short-lived feature branches, frequent merges), GitHub Flow (feature branch → PR → merge to main), GitFlow (develop, feature, release, hotfix branches), release flow, choosing a strategy by team size and release cadence, branch naming conventions
   - **Commit conventions** — Conventional Commits (feat, fix, docs, style, refactor, perf, test, chore, scope), commit message structure (type, scope, description, body, footer), breaking change notation, automated changelog generation from commits, commit hooks for validation, squash vs merge commits
   - **PR/MR workflow** — PR description templates, PR review workflow (request review, review, address feedback, approve, merge), draft PRs, stacked PRs (for large features), PR size guidelines, review turnaround expectations, auto-merge configuration, branch protection rules, required reviews, CI checks as merge gates
   - **Rebase vs merge** — when to rebase (clean history, before merging feature branch), when to merge (preserve history, integration commits), interactive rebase (squash, fixup, reorder, drop commits), rebase conflicts, force push safety (--force-with-lease), golden rule of rebasing (never rebase public branches)
   - **Advanced git** — git bisect for finding regressions, git cherry-pick for hotfixes, git reflog (safety net for mistakes), git stash (save work in progress), git worktrees (multiple branches checked out simultaneously), git submodules vs subtrees, git LFS for large files, partial clone and sparse checkout
   - **Conflict resolution** — understanding merge conflicts, conflict markers, resolving conflicts in code, conflict resolution strategies (combine both, pick one, rewrite), conflict during rebase vs merge, conflict prevention (small PRs, frequent sync with main), conflict resolution tools (VS Code, merge tools)
   - **Monorepo git** — monorepo branching strategies, code ownership (CODEOWNERS file), path-based CI (only run tests for changed packages), atomic commits across packages, monorepo PR patterns, sparse checkout for large monorepos
   - **Git hooks** — pre-commit (lint, format, type check), commit-msg (conventional commit validation), pre-push (run tests), husky for hook management, lint-staged for running tools on staged files only, skipping hooks (--no-verify, when it's appropriate), shared hooks with .husky directory
   - **Release management** — tagging releases (semantic versioning tags), release branches, hotfix workflow, release notes generation, GitHub Releases, automated release pipelines (changesets, release-please), rollback by reverting merge commit, release candidate management
   - **Git security** — secrets in git history (preventing with pre-commit hooks, git-secrets), removing secrets from history (BFG, git filter-repo), signed commits (GPG, SSH signing), branch protection, requiring PR reviews, CODEOWNERS, dependency review in PRs

   the skill should reflect modern 2025-2026 git practices — trunk-based development, Conventional Commits, squash merges, stacked PRs, and automated release pipelines.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 27

1) Download a **"build tools & bundlers" skill** — a comprehensive methodology for configuring, optimizing, and managing build tooling for any web project I create in this project and future projects. It must cover:

   - **Bundler selection** — Vite (fast dev server, esbuild + Rollup, modern default), Turbopack (Next.js native, Rust-based), webpack (legacy, maximum ecosystem), esbuild (extremely fast, limited), Rollup (library builds, tree-shaking), Parcel (zero-config), Rspack (webpack-compatible, Rust-based), choosing by project type
   - **Vite configuration** — vite.config.ts structure, plugins (React, Vue, Svelte, legacy browser support, PWA, compression), resolve aliases, environment variables (import.meta.env), build options (target, minify, sourcemap, chunkSizeWarningLimit), server options (port, proxy, HTTPS), CSS preprocessing, asset handling
   - **Hot Module Replacement (HMR)** — HMR mechanics (module graph, invalidation, hot accept), React Fast Refresh (preserving state during HMR), HMR for CSS (style injection without reload), HMR for other frameworks, HMR edge cases and failures, HMR performance, debugging HMR issues
   - **Code splitting** — route-based splitting (dynamic imports per route), component-based splitting (lazy-load heavy components), vendor splitting (separate node_modules chunks), manual chunk configuration, dynamic import syntax, import() with magic comments (webpackChunkName, prefetch, preload), analyzing bundle output
   - **Tree shaking** — dead code elimination, sideEffects field in package.json, ESM vs CJS for tree shaking, verifying tree shaking in bundle output, tree shaking with barrel files (index.ts re-exports), tree shaking pitfalls (side effects, dynamic imports, class methods)
   - **Source maps** — source map types (eval, cheap-module-source-map, source-map, hidden-source-map), production source maps (for error tracking, not served publicly), source map security (preventing source exposure), source map upload to Sentry, source map debugging in DevTools, source maps for monorepos
   - **Asset handling** — image imports (asset module, inline threshold), font handling, SVG handling (file, inline, component), CSS handling (CSS Modules, PostCSS, Tailwind, Sass, Less), JSON imports, Web Workers, WebAssembly, static assets in public/ vs imported assets
   - **Build optimization** — minification (esbuild, terser, swc), CSS minification (lightningcss, cssnano), asset inlining (base64 for small assets), gzip and brotli compression (vite-plugin-compression), bundle analysis (rollup-plugin-visualizer), chunk size warnings, legacy browser support (@vitejs/plugin-legacy)
   - **Environment management** — .env files (.env, .env.local, .env.production, .env.development), environment variable exposure to client (VITE_ prefix, NEXT_PUBLIC_ prefix), secret management (never expose server secrets), environment validation (zod env schema), runtime config vs build-time config
   - **Monorepo builds** — Turborepo task orchestration (build, lint, test pipelines), caching (local cache, remote cache), task dependencies (topological build order), Nx for monorepo builds, pnpm workspace builds, shared build configs, package-level build overrides

   the skill should reflect modern 2025-2026 build practices — Vite as default, Turbopack for Next.js, esbuild for speed, Rollup for libraries, and remote caching for monorepos.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 28

1) Download a **"type safety & TypeScript" skill** — a comprehensive methodology for designing type-safe applications and leveraging TypeScript for any web project I create in this project and future projects. It must cover:

   - **TypeScript configuration** — tsconfig.json structure, strict mode (strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes), module resolution (node, bundler, nodenext), target and lib settings, path aliases, project references for monorepos, composite projects, incremental builds, declaration files
   - **Type design patterns** — discriminated unions (tagged unions for state modeling), branded types (nominal typing in TypeScript), result types (Result<T, E> instead of throwing), option types (Option<T> for null safety), type branding for IDs (UserId, OrderId), phantom types, newtype pattern
   - **Advanced types** — conditional types (T extends U ? X : Y), mapped types ({ [K in keyof T]: ... }), template literal types (`${Prefix}-${string}`), inference with infer, type predicates (x is Foo), satisfies operator, const type parameters, recursive types, type-level programming
   - **Utility types** — built-in (Partial, Required, Pick, Omit, Record, ReturnType, Parameters, Awaited), custom utility types (DeepPartial, Mutable, DeepReadonly, Paths, RequiredKeys, OptionalKeys), type-safe object manipulation, type-safe function composition
   - **Runtime validation** — Zod (schema definition, parsing, safeParse, transforms, refinements, discriminated unions, .partial(), .merge()), Valibot (lighter alternative), TypeBox (JSON Schema-compatible), io-ts, runtime validation at API boundaries, inferring types from schemas (z.infer)
   - **Type-safe APIs** — tRPC (end-to-end type safety, procedure definition, routers, context, middleware, error handling), OpenAPI codegen (openapi-typescript, openapi-fetch), GraphQL codegen (typed operations from schema), gRPC-web with TypeScript, type-safe fetch wrappers
   - **Generics** — generic functions, generic classes, generic constraints (extends), conditional type inference, generic defaults, variadic tuples, generic type inference pitfalls, when to use generics vs overloads, generic component patterns in React
   - **Type narrowing** — typeof guards, instanceof, in operator, user-defined type guards (type predicates), discriminated union narrowing, exhaustive checks (never type), assertion functions, control flow analysis, narrowing with arrays and methods
   - **React + TypeScript** — component prop types, hook return types, event handler types, ref types, context types, polymorphic components (generics with as prop), component composition types, children types, forwardRef generics, type-safe route params, type-safe environment variables
   - **Type-safe error handling** — Result type pattern (success/error without exceptions), error as values, typed errors (error union types), never throwing in library code, error mapping at boundaries, type-safe try-catch alternatives, exhaustive error handling

   the skill should reflect modern 2025-2026 TypeScript practices — strict mode everywhere, Zod for runtime validation, tRPC for end-to-end type safety, branded types for domain modeling, and discriminated unions for state.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 29

1) Download a **"CSS architecture" skill** — a comprehensive methodology for designing, organizing, and maintaining CSS at scale for any web project I create in this project and future projects. It must cover:

   - **CSS methodologies** — BEM (Block Element Modifier, naming conventions), CUBE CSS (composition, utility, block, exception), ITCSS (inverted triangle, specificity ordering), Atomic CSS (Tailwind approach), CSS Modules (scoped by default), when to use each, mixing methodologies
   - **Tailwind CSS architecture** — Tailwind configuration (theme extension, custom colors, fonts, spacing), Tailwind v4 (CSS-first config, @theme directive, native CSS variables), utility composition patterns, @apply usage (and when to avoid), custom utilities, variant design, plugin development, Tailwind in monorepos, performance considerations
   - **CSS custom properties** — design tokens as CSS variables, variable inheritance, scoped variables, variable cascading, runtime theme switching with variables, variable fallbacks, type-safe CSS variables, custom properties vs preprocessor variables, performance of CSS variables
   - **Modern CSS features** — container queries (@container, container-type, container-name), cascade layers (@layer), :has() selector (parent selector), nesting (native CSS nesting), subgrid, aspect-ratio, object-fit/object-position, scroll-snap, scroll-driven animations, text-wrap: balance, view transitions CSS
   - **Responsive design** — mobile-first methodology, fluid typography with clamp(), fluid spacing, container queries for component responsiveness, breakpoint strategy (min-width vs max-width), responsive images in CSS, responsive grids, responsive flexbox, viewport units (dvh, svh, lvh)
   - **Dark mode** — dark mode architecture (class-based vs prefers-color-scheme), CSS variables for theming, dark mode color adjustment (reducing contrast, not just inverting), dark mode images (dimming, different images), dark mode shadows, dark mode transitions, system preference detection, manual toggle with persistence
   - **CSS-in-JS** — styled-components, Emotion, Vanilla Extract (zero-runtime, type-safe), Panda CSS (build-time, type-safe), Stitches, when CSS-in-JS adds value vs overhead, runtime vs zero-runtime tradeoffs, CSS-in-JS in server components, migration from runtime CSS-in-JS to zero-runtime
   - **CSS performance** — critical CSS extraction (above-the-fold CSS inlined), CSS size budgets, unused CSS removal (PurgeCSS, Tailwind's content config), CSS specificity management, selector performance (avoiding expensive selectors), will-change, contain (CSS containment), content-visibility: auto, CSS and render performance
   - **CSS organization** — file structure (by component, by feature, by layer), CSS import order, CSS module naming, BEM file naming, stylelint configuration, CSS code review checklist, CSS documentation, CSS refactoring strategies, managing CSS debt

   the skill should reflect modern 2025-2026 CSS practices — Tailwind v4, native CSS features (container queries, @layer, :has(), nesting), zero-runtime CSS-in-JS, and CSS custom properties as the theming foundation.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 30

1) Download a **"developer experience (DX)" skill** — a comprehensive methodology for setting up and optimizing the development experience for any web project I create in this project and future projects. It must cover:

   - **Project scaffolding** — project initialization (create-next-app, create-vite, degit templates), project structure conventions, boilerplate elimination, starter templates, monorepo scaffolding, shared config packages (eslint config, prettier config, tsconfig), scaffolding tools (Plop, Hygen)
   - **Development server** — dev server configuration (Vite, Next.js, webpack dev server), fast refresh/HMR setup, dev server proxy (API proxy to backend), HTTPS in development (mkcert), dev server performance, multi-app dev server (monorepo), dev server debugging
   - **Code formatting** — Prettier configuration (printWidth, tabWidth, semi, singleQuote, trailingComma), Prettier plugins (prettier-plugin-tailwindcss, prettier-plugin-organize-imports), format on save (VS Code), format on commit (lint-staged + prettier), Prettier vs Biome formatting, ignoring files (.prettierignore)
   - **Linting** — ESLint flat config (eslint.config.js), ESLint rules (react, react-hooks, jsx-a11y, import, unicorn, sonarjs), Biome as all-in-one alternative (lint + format, Rust-based, fast), lint rules for code quality, lint rules for accessibility, lint rules for imports, lint-staged for running on changed files only
   - **Editor configuration** — VS Code settings.json (format on save, default formatter, tab size), recommended extensions (.vscode/extensions.json), workspace settings, launch.json for debugging, tasks.json for custom tasks, editor config (.editorconfig for cross-editor consistency), TypeScript in-editor features
   - **Pre-commit hooks** — Husky setup (.husky directory, hook scripts), lint-staged (run formatters/linters on staged files), commitlint (enforce Conventional Commits), commitizen for interactive commit creation, pre-commit type checking, pre-commit test running, hook performance optimization
   - **Error overlay & debugging** — error overlay configuration (Vite error overlay, Next.js error overlay), React DevTools, Redux DevTools, React Query Devtools, browser DevTools setup, debugging configuration (VS Code debugger, Chrome DevTools Protocol), source map debugging, Node.js debugging (inspect flag)
   - **Development documentation** — README.md (project overview, setup, scripts, conventions), CONTRIBUTING.md (how to contribute, PR process, code style), DEVELOPMENT.md (local setup, environment variables, database setup), architecture documentation, ADRs (Architecture Decision Records), inline documentation standards
   - **Onboarding** — one-command setup (makefile, package.json scripts), environment variable templates (.env.example), database seeding for development, demo data generation, onboarding checklist, first PR guidance, pair programming setup, development environment troubleshooting guide
   - **Developer productivity** — snippet management (VS Code snippets, Snippets folder), keyboard shortcuts, command palette usage, multi-cursor editing, Emmet abbreviations, AI-assisted development (Copilot, Cursor, Windsurf), task runners (npm scripts, Makefile, just), CLI tools (tsx, ts-node, esno)

   the skill should reflect modern 2025-2026 DX practices — Biome as fast all-in-one, ESLint flat config, Husky + lint-staged, one-command onboarding, and AI-assisted development.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 31

1) Download a **"documentation" skill** — a comprehensive methodology for writing, organizing, and maintaining documentation for any web project I create in this project and future projects. It must cover:

   - **Code documentation** — JSDoc/TSDoc comments (functions, classes, interfaces, types), documentation for public APIs vs internal code, when to document vs when code is self-documenting, inline comment guidelines (explain why, not what), TODO/FIXME conventions, deprecated annotations, @see, @example, @param, @returns
   - **API documentation** — OpenAPI/Swagger specification (paths, components, security, examples), Swagger UI / Redoc for interactive docs, API documentation generation from code (tsoa, nestia), API changelog, API versioning in docs, request/response examples, error code documentation, authentication documentation
   - **README writing** — project overview (what it does, why it exists), installation instructions, usage examples, configuration reference, scripts reference, prerequisites, badges (CI, coverage, version), license, contributing link, table of contents for long READMEs, README for npm packages
   - **Architecture Decision Records (ADRs)** — ADR format (title, status, context, decision, consequences), when to write an ADR, ADR storage (docs/adr/, markdown files), ADR numbering, superseding ADRs, ADR templates, linking ADRs from code, ADR review process
   - **Documentation sites** — Docusaurus (React-based, MDX, versioned docs), Nextra (Next.js-based, Markdown), Mintlify (hosted, beautiful defaults), Fumadocs (Next.js, modern), Astro Starlight, documentation site architecture, search in docs, versioned documentation, documentation analytics
   - **Diagrams** — Mermaid diagrams (flowchart, sequence, class, state, ER, C4), Excalidraw for hand-drawn style, diagram-as-code, architecture diagrams, sequence diagrams for API flows, ER diagrams for database schema, deployment diagrams, embedding diagrams in Markdown/MDX
   - **Changelog & release notes** — Keep a Changelog format (Added, Changed, Deprecated, Removed, Fixed, Security), automated changelog from Conventional Commits (changesets, release-please), user-friendly release notes vs developer changelog, breaking change highlights, migration guides in release notes
   - **Contribution guides** — CONTRIBUTING.md structure, code of conduct, PR process, code style requirements, testing requirements, review process, branch naming, commit conventions, issue templates, PR templates, good first issues, development setup for contributors
   - **Documentation as code** — docs live in the repo, docs versioned with code, docs in CI (build, test links, lint Markdown), docs deployment from CI, MDX for interactive docs, live code examples in docs, documentation testing (remark, markdownlint), dead link detection
   - **User-facing documentation** — help center articles, FAQ writing, tutorial writing, onboarding guides, feature documentation, troubleshooting guides, video documentation, screenshot/ GIF documentation, user documentation analytics, feedback collection

   the skill should reflect modern 2025-2026 documentation practices — docs as code, MDX for interactivity, Mermaid for diagrams, ADRs for decisions, and automated changelog generation.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 32

1) Download a **"monorepo management" skill** — a comprehensive methodology for setting up, organizing, and managing monorepos for any web project I create in this project and future projects. It must cover:

   - **Monorepo tools** — Turborepo (task orchestration, caching, remote cache, daemon), Nx (generators, executors, affected commands, project graph), pnpm workspaces (workspace protocol, overrides, patching), Lerna (legacy, maintenance mode), choosing a tool by project complexity, Turborepo vs Nx tradeoffs
   - **Workspace structure** — apps/ and packages/ structure, shared packages (ui, config, utils, types, eslint-config, tsconfig), app-specific packages, package naming conventions, package versioning strategy (fixed, independent), internal package imports (workspace:*), public vs private packages
   - **Task orchestration** — defining tasks (build, lint, test, dev), task dependencies (topological order, build package A before app that depends on it), task pipeline configuration (Turborepo pipeline, Nx target dependencies), parallel task execution, task caching (inputs, outputs, cache key), remote caching setup
   - **Dependency management** — pnpm workspace protocol (workspace:*), hoisting strategy (pnpm strict by default), shared dependencies vs per-package dependencies, dependency version alignment, dependency overrides, pnpm patches for fixing packages, catalog (pnpm catalog for centralized versions)
   - **Shared configurations** — shared ESLint config (eslint-config-custom), shared TypeScript config (tsconfig-custom with base, nextjs, react variants), shared Prettier config, shared Tailwind config, shared Jest/Vitest config, shared CI workflows, sharing via workspace package
   - **Building monorepos** — package build order (dependency graph), incremental builds (only build changed packages and dependents), build caching (Turborepo cache, Nx cache), build outputs, package exports (exports field in package.json), dual package (ESM/CJS), build watching (turborepo watch mode)
   - **CI/CD for monorepos** — affected command (only test/build changed packages and dependents), path-based CI filters, matrix builds (parallel jobs per package), monorepo deployment (deploy specific apps), preview deployments per app, CI caching strategies, CI for shared packages, CI parallelization
   - **Publishing** — changesets (managing versions and changelogs, changeset workflow, publishing), release-please, Nx release, publishing private packages, publishing scoped packages (@scope/name), npm publishing from monorepo, publishing only changed packages, version bumping strategies
   - **Code sharing patterns** — shared UI component library, shared utilities, shared types/schemas, shared hooks, shared constants, shared test utilities, barrel files (index.ts) tradeoffs, tree-shaking in monorepos, internal packages (not published, only used within monorepo)
   - **Monorepo challenges** — IDE performance (TypeScript project references, incremental mode), import speed (pnpm hard links), circular dependency detection, enforcing package boundaries (eslint-plugin-boundaries, import/no-unresolved), package ownership (CODEOWNERS per package), monorepo scaling strategies

   the skill should reflect modern 2025-2026 monorepo practices — Turborepo + pnpm workspaces as default, changesets for publishing, remote caching for speed, and strict package boundaries.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 33

1) Download an **"email & notifications" skill** — a comprehensive methodology for implementing transactional emails, notifications, and communication flows for any web project I create in this project and future projects. It must cover:

   - **Transactional email** — email service selection (Resend, SendGrid, Postmark, AWS SES, Mailgun), email sending patterns (server-side only, never client), email templates (React Email, MJML, Handlebars, Plain text + HTML), email testing (Ethereal Email, Mailtrap, Resend preview), email queue (background jobs for sending), batch sending, rate limiting
   - **Email template design** — React Email (component-based, reusable, type-safe), MJML (responsive email markup), HTML email constraints (table-based layout, inline CSS, client compatibility), plain text alternative (always include), email dark mode, email accessibility, responsive email design, email preview text, email images (hosted, not embedded)
   - **Email deliverability** — SPF (Sender Policy Framework), DKIM (DomainKeys Identified Mail), DMARC (Domain-based Message Authentication), DNS record setup, sender reputation, warmup for new domains, bounce handling, complaint handling, unsubscribe links (List-Unsubscribe header), email authentication for transactional vs marketing
   - **Push notifications** — Web Push API (service worker, push subscription, VAPID keys), Firebase Cloud Messaging (FCM for cross-platform), Apple Push Notification service (APNs), push notification permissions (timing, copy, denial recovery), notification payload design, notification actions, notification analytics, push notification testing
   - **In-app notifications** — notification bell UI, notification dropdown/panel, notification list, read/unread state, notification badges (count, dot), notification real-time delivery (WebSocket, SSE), notification types (info, success, warning, error, social), notification grouping, notification persistence
   - **Notification routing** — user notification preferences (email, push, in-app, SMS), notification frequency controls (instant, daily digest, weekly digest), do-not-disturb hours, notification priority levels, notification routing logic (which channel for which event), notification suppression (don't notify for user's own actions)
   - **SMS notifications** — Twilio, Vonage/Nexmo, AWS SNS, SMS template design (160 char limit, no HTML), SMS compliance (opt-in/opt-out, STOP keyword), SMS rate limiting, SMS delivery receipts, international SMS considerations, SMS vs push tradeoffs
   - **Notification templates** — template management (event → template mapping), template variables/interpolation, template versioning, template A/B testing, template localization (multi-language), template preview, template testing, template approval workflow
   - **Digest & batching** — daily digest emails (collect notifications, send once per day), weekly digest, notification batching (group similar notifications), digest template design, digest scheduling (cron jobs, queues), digest unsubscribe, digest analytics
   - **Notification analytics** — email open tracking (pixel, privacy concerns), email click tracking (UTM links), push notification delivery rate, push notification click rate, notification engagement metrics, unsubscribe rate, notification A/B testing, notification fatigue detection

   the skill should reflect modern 2025-2026 email & notification practices — Resend + React Email for transactional, Web Push for browser notifications, user-controlled notification preferences, and deliverability-first email strategy.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.


# Prompt 34

1) Download a **"web scraping & data collection" skill** — a comprehensive methodology for ethically and efficiently collecting data from the web for any web project I create in this project and future projects. It must cover:

   - **Ethical scraping** — respecting robots.txt, terms of service review, rate limiting (be a good citizen, 1 req/sec default), identifying yourself (User-Agent), avoiding server overload, public vs private data, legal considerations (CFAA, GDPR), scraping vs API usage (always prefer API), ethical scraping guidelines
   - **Scraping tools** — Playwright/Puppeteer (for JavaScript-rendered pages, headless browser), Cheerio (HTML parsing, jQuery-like API, fast), jsdom (DOM simulation in Node.js), HTTP clients (fetch, axios, got, undici), scraping frameworks (Crawlee, Scrapy-equivalent for Node), when to use headless vs static parsing
   - **Data extraction** — CSS selectors (class, id, attribute, nth-child), XPath expressions, regex for structured data, JSON-LD extraction (structured data in pages), Open Graph metadata extraction, table extraction, pagination handling (next page links, infinite scroll, page numbers), data cleaning and normalization
   - **JavaScript-rendered pages** — detecting JS-rendered content, Playwright page.waitForSelector, Playwright page.evaluate, handling SPAs (React, Vue, Angular), waiting for network idle, intercepting API calls (Playwright route interception), extracting data from XHR/fetch responses, bypassing unnecessary rendering
   - **Anti-bot handling** — CAPTCHA (ethical considerations, 2Captcha, manual solving), Cloudflare challenge, rate limit detection (429, 503), rotating user agents, request headers (Accept, Accept-Language, Referer), session management, cookie handling, request throttling, exponential backoff on blocks
   - **Scraping architecture** — scraping pipeline (fetch → parse → clean → store), queue-based scraping (BullMQ, Redis queue), concurrent scraping (worker pool, concurrency limits), scraping scheduler (cron, interval), incremental scraping (only new data), deduplication, scraping monitoring and alerting
   - **Data storage** — storing scraped data (PostgreSQL, MongoDB, JSON files, CSV), data schema for scraped content, incremental storage (upsert vs insert), data deduplication strategies, raw data storage (S3, file system) for reprocessing, data export formats (JSON, CSV, XML), data backup
   - **Proxy management** — proxy rotation (residential, datacenter), proxy pools, proxy health checking, geo-distributed proxies, proxy authentication, when proxies are necessary, proxy cost optimization, avoiding IP bans, proxy-as-a-service (Bright Data, Smartproxy)
   - **Scraping at scale** — distributed scraping (multiple workers, multiple machines), scraping with serverless (AWS Lambda, Cloudflare Workers), scraping with containers (Docker, Kubernetes), handling millions of pages, scraping throughput optimization, cost per page optimization, scraping infrastructure
   - **API alternatives** — finding official APIs before scraping, RSS feeds, data dumps, public datasets, government data portals, third-party data providers, scraping vs API cost analysis, hybrid approach (API for what's available, scrape for the rest)

   the skill should reflect modern 2025-2026 scraping practices — Playwright for JS-rendered pages, Crawlee for orchestration, ethical rate limiting, API-first approach, and queue-based scraping architecture.

2) Once downloaded, install it as a **global skill** so it is available across all projects without re-downloading.

3) Convert that skill into a **global workflow** so it can be invoked across projects without re-downloading.

4) Finally, add a **global rule** that mandates following this skill and workflow whenever we start a new project — making the entire setup persistent and reusable across all future projects.